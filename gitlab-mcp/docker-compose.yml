services:
  gitlab-mcp:
    build: .
    container_name: gitlab-mcp
    ports:
      - "${MCP_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    stdin_open: true
    tty: true
