services:
  jira-tempo:
    build: .
    container_name: jira-tempo
    ports:
      - "${MCP_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    stdin_open: true
    tty: true
